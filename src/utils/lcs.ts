/**
 * Finds the positions of the Longest Common Subsequence between two strings
 * This will be used to determine which characters are common between a misspelled word and its correct form
 * 
 * @param misspelled The misspelled word
 * @param correct The correctly spelled word
 * @returns An array of position pairs indicating matching characters
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function findLCSPositions(misspelled: string, correct: string): Array<[number, number]> {
  // To be implemented in task 2
  return [];
} 